// Placeholder so Tabs can register the "log" route. Never displays:
// the tab uses a custom tabBarButton that opens the modal at /(app)/log/new.
export default function LogTabPlaceholder() {
  return null;
}
